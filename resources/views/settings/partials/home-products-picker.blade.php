<div class="hb-panel"><div class="hb-panel-title"><h3>{{ $title }}</h3><small>Se usan cuando eliges selección manual.</small></div><div class="hb-check-grid">@foreach($storeProducts as $product)<label><input type="checkbox" name="content[product_ids][]" value="{{ $product->id }}" @checked(in_array($product->id,$content['product_ids']??[]))> {{ $product->name }}</label>@endforeach</div></div>
