@extends('tienda::layouts.storefront')
@section('title', $article['title'].' · '.$project->name)
@section('content')
@php $image=$article['image']??null;$image=$image?(str_starts_with($image,'http')?$image:asset('storage/'.$image)):null; @endphp
<style>.article-shell{padding:clamp(42px,7vw,82px) 0;background:#f8fafc}.article-wrap{max-width:850px;margin:auto}.article-back{display:inline-flex;margin-bottom:22px;color:var(--store-primary);font-weight:700;text-decoration:none}.article-card{overflow:hidden;border:1px solid #e2e8f0;border-radius:13px;background:#fff}.article-card img{width:100%;max-height:480px;object-fit:cover}.article-copy{padding:clamp(24px,5vw,55px)}.article-copy small{color:var(--store-primary);font-weight:800;text-transform:uppercase}.article-copy h1{margin:10px 0 18px;font:800 clamp(34px,5vw,56px)/1.08 var(--store-font-title);letter-spacing:-.04em}.article-copy p{color:#526176;font-size:17px;line-height:1.8;white-space:pre-line}</style><section class="article-shell"><div class="store-container article-wrap"><a class="article-back" href="{{ route('public.blog',$project->slug) }}">← Volver al blog</a><article class="article-card">@if($image)<img src="{{ $image }}" alt="{{ $article['title'] }}">@endif<div class="article-copy">@if($article['tag']??null)<small>{{ $article['tag'] }}</small>@endif<h1>{{ $article['title'] }}</h1><p>{{ $article['body']??$article['summary']??'' }}</p></div></article></div></section>
@endsection
